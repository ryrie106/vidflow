spring:
  jpa:
    show_sql: true
    database: MYSQL
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://vidflow-db.c3sgswc5qc63.ap-northeast-2.rds.amazonaws.com:3306/vidflow
    username: mysqladmin
    password: mysqlpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      # Enable multipart uploads
      enabled: true
      # Threshold after which files are written to disk.
      file-size-threshold: 2KB
      # Max file size.
      max-file-size: 100MB
      # Max Request Size
      max-request-size: 125MB

file:
  upload-dir: /home/ubuntu/www/videos

app:
  jwtSecret: JWTSuperSecretKey
  jwtExpirationInMs: 604800000

