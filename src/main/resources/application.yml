spring:
  jpa:
    show_sql: false
    database: MYSQL
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://vidflow-db.c3sgswc5qc63.ap-northeast-2.rds.amazonaws.com/vidflow
    username: mysqladmin
    password: mysqlpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
#    initialization-mode: always
server:
  port: 8080
#  ssl:
#    enabled: true
#    key-store: /home/ubuntu/key/vidflow.jks
#    key-store-password: sslpassword
#    key-password: sslpassword

app:
  jwtSecret: JWTSuperSecretKey
  jwtExpirationTime: 3600000 # 1hour
  s3BucketName: vidflow
  s3UploadURLExpirationTime: 300000 #5mins

